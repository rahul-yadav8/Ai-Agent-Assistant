import React from "react";
import { Text, View } from "react-native";

const Profile = () => {
  return (
    <View className="bg-[red] flex-1 justify-center items-center">
      <Text className="text-[50px] text-red-500">Profile</Text>
    </View>
  );
};

export default Profile;
